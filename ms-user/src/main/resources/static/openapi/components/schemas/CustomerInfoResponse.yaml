type: object
required: [id, email, name]
properties:
  id:
    type: integer
    format: int64
    example: 10
  email:
    type: string
    format: email
    example: "cliente@exemplo.com"
  name:
    type: string
    example: "Cliente Exemplo"
