get:
  tags: [Despesas]
  summary: Total por mês
  description: >
    Retorna a soma de despesas agrupada por mês no intervalo informado.
    Requer autenticação e ownership do userId.
  operationId: expenseTotalByMonth
  security:
    - bearerAuth: []
  parameters:
    - $ref: "../../components/parameters.yaml#/UserIdPath"
    - $ref: "../../components/parameters.yaml#/StartDateParam"
    - $ref: "../../components/parameters.yaml#/EndDateParam"
    - $ref: "../../components/parameters.yaml#/PageParam"
    - $ref: "../../components/parameters.yaml#/SizeParam"
    - $ref: "../../components/parameters.yaml#/SortParam"
  responses:
    '200':
      description: Lista de totais por mês
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas/ExpenseByMonth.yaml"
    '401':
      $ref:  "../../components/responses.yaml#/Unauthorized"
    '403':
      $ref:  "../../components/responses.yaml#/Forbidden"