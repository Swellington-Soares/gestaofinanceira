type: object
required: [amount, currencyType]
properties:
  amount:
    type: number
    format: double
    exclusiveMinimum: true
    minimum: 0
    description: Valor da transação (deve ser maior que zero)
    example: 150.75
  currencyType:
    $ref: ./CurrencyType.yaml
